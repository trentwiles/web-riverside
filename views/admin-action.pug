include head.pug
h1 #{user} - Settings
br
h1 Delete Account
p Please note this cannot be undone!
form(method="post" class="w-400 mw-full")
div(class="form-group")
        label(for="bio" class="required") Short Reason for Ban
        input(type="text" class="form-control" id="bio" name="bio" placeholder="This user..." required="required")
    div(class="form-group")
        label(for="bio" class="required") Long Reason for Ban
        input(type="text" class="form-control" id="bio" name="bio" placeholder="This user..." required="required")
    div(class="form-group")
        div(class="custom-checkbox")
            input(type="checkbox" id="messages")
            label(for="messages") Delete all messages
    div(class="form-group")
        div(class="custom-checkbox")
            input(type="checkbox" id="ipban")
            label(for="ipban") Also ban by IP (#{ip})
    input(class="btn btn-primary" type="submit" value="Terminate User")